import React from 'react';
import s from './ImagePreview.module.scss';

const ImagePreview: React.FC = () => {
	return <div className={s.container}>ImagePreview</div>;
};

export default ImagePreview;
